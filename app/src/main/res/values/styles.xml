<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <color name="custom_theme_bg">#5A0003</color>
    <color name="custom_theme_text">#FFFFFF</color>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/custom_theme_bg</item>
        <item name="android:colorBackground">@color/custom_theme_bg</item>
        <item name="android:textColor">@color/custom_theme_text</item>
    </style>

</resources>


